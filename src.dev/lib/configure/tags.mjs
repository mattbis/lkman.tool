// parse [thing] when using commands as 2nd argument ( after command ) 
// can only be a-z, 0-6, _ - 
// parsed as a safe string with stripping of any chars that arent pure ascii
// TODO (matt): this should be release.

/** tags are how everything works...  you can do someting without a tag... by default they sit in one default tag - its much better to use
custom stags.. to reduce the operation workload ... and what lib-change-descent is doing */
