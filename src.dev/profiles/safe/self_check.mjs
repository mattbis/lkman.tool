/** when in safe mode performs a real file system test to ensure it actually works 
 * and something weird is not happening */

export function self_check() {
    // use vfs, expand to temp and another disk.. link test , and then remove ( hmm danger )    
}
